{
  "name": "setup-edgedb",
  "version": "1.0.0",
  "private": true,
  "description": "EdgedDB Setup Github Action",
  "type": "module",
  "main": "dist/index.cjs",
  "scripts": {
    "build": "esbuild main.ts --outfile=dist/index.cjs --bundle --platform=node --format=cjs --sourcemap --minify"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.2.2"
  },
  "author": "didimon",
  "license": "ISC"
}
